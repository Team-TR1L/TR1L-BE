spring:
  application:
    name: tr1l-billing

  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}

---

spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate: create-drop