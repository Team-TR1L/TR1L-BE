spring:
  datasource:
    url: ${PG_SUB_HOST:jdbc:postgresql://localhost:5433/billing_target}
    username: ${PG_SUB_USER:postgres}
    password: ${PG_SUB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  # ... (이하 생략)

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
