target:
  datasource:
    url: ${TARGET_DATASOURCE_URL:jdbc:postgresql://localhost:5433/billing_target}
    username: ${TARGET_DATASOURCE_USERNAME:billing}
    password: ${TARGET_DATASOURCE_PASSWORD:billing}
    driver-class-name: org.postgresql.Driver